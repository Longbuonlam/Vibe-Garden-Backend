# Copy this file to `.env` and fill in real values. Do NOT commit your real `.env` file.

# Gmail account (used by nodemailer in contact and place-order routes)
GMAIL_USER=your-email@gmail.com
# Use an app password for Gmail (recommended) or your SMTP password
GMAIL_PASS=your-app-password

# Optional: custom ping message returned by GET /api/ping
PING_MESSAGE=ping
